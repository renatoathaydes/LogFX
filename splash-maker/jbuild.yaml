imports:
  - "../resources/build-properties.yaml"

group: com.athaydes.logfx
module: logfx-jb-extension
version: "{{versions.logfx}}"

source-dirs: [ src/main/java, ../src/common/java ]
resource-dirs: [ ../src/common/resources, ../resources ]

run-java-args: ["-Djava.awt.headless=true"]

dependencies:
  com.athaydes.jbuild:jbuild-api:{{versions.jbuild-api}}:
    scope: compile-only
